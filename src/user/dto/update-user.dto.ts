// import { Types } from 'mongoose';

export class UpdateUserDto {
  private telegramNickname?: string;
  private firstName?: string;
  private lastName?: string;
  private phone?: string;
  // $push?: { purchases?: Types.ObjectId[] };
}
